#include <string>
#include <vector>

namespace aoc {

std::vector<std::string> split_string_by_character(const std::string& s,
                                                   char splitter);
}  // namespace aoc
